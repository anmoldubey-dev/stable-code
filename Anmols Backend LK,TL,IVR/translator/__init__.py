# translator — Real-Time Bidirectional Speech Translation Microservice
# Run: python -m uvicorn app:app --port 9000 (from inside translator/)
#   OR: python -m uvicorn translator.app:app --port 9000 (from project root)
